// export const projectileWidth = 7;
// const projectileHeight = 14;
// export const projectileSpeed = 9;

export default class Projectile {
    constructor(x, y, projectileWidth, projectileHeight) {
      this.x = x;
      this.y = y;
      this.width = projectileWidth;
      this.height = projectileHeight;
    }
  };
